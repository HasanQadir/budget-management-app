# Django Settings
DEBUG=True
SECRET_KEY=your-dev-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=budget_dev
DB_USER=dev_user
DB_PASSWORD=dev_password
DB_HOST=localhost
DB_PORT=5432

# Redis/Celery
REDIS_URL=redis://localhost:6379/0

# Email Settings
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=no-reply@dev.example.com

# Security (set to False in development, True in production)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# Logging
LOG_LEVEL=DEBUG

# AWS (if used)
AWS_ACCESS_KEY_ID=your-dev-aws-key
AWS_SECRET_ACCESS_KEY=your-dev-aws-secret
AWS_STORAGE_BUCKET_NAME=budget-dev-bucket
